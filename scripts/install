#!/bin/bash

#=================================================
# IMPORT GENERIC HELPERS
#=================================================

source _common.sh
source /usr/share/yunohost/helpers

#=================================================
# INITIALIZE AND STORE SETTINGS
#=================================================

# Settings are written both in app's settings.yml and in $install_dir/js/config.js via a custom setter (cf. app scripts/config). They are read from settings.yml only.
# Variables names initialized below are used as template tags to populate config.js entries during `ynh_config_add` command below.
archives_paths=""
ynh_app_setting_set --key=archivesPaths --value=$archives_paths

lang="en"
ynh_app_setting_set --key=lang --value="\"$lang\""

theme="light"
ynh_app_setting_set --key=theme --value="\"$theme\""

sort_asc=true
ynh_app_setting_set --key=sortAsc --value=$sort_asc

page_size=10
ynh_app_setting_set --key=pageSize --value=$page_size

combine_panels=false
ynh_app_setting_set --key=combinePanels --value=$combine_panels

default_panels=null
ynh_app_setting_set --key=defaultPanels --value=$default_panels

simplify_post_display=false
ynh_app_setting_set --key=simplifyPostDisplay --value=$simplify_post_display

images_compact_layout=false
ynh_app_setting_set --key=imagesCompactLayout --value=$images_compact_layout

hide_no_alt_text_notice=false
ynh_app_setting_set --key=hideNoAltTextNotice --value=$hide_no_alt_text_notice

loop_gifs=true
ynh_app_setting_set --key=loopGifs --value=$loop_gifs

show_actor_json=false
ynh_app_setting_set --key=showActorJson --value=$show_actor_json

show_posts_json=false
ynh_app_setting_set --key=showPostsJson --value=$show_posts_json

app_title="Mastodon Archive Reader Lite - MARL"
ynh_app_setting_set --key=appTitle --value="\"$app_title\""

#=================================================
# DOWNLOAD, CHECK AND UNPACK SOURCE
#=================================================
ynh_script_progression "Setting up source files..."

temp_dir=$(mktemp -d)
ynh_setup_source --dest_dir="$temp_dir"
mv $temp_dir/dist/* $install_dir #keep only the content of `dist` folder
ynh_safe_rm $temp_dir

#=================================================
# APP INITIAL CONFIGURATION
#=================================================
ynh_script_progression "Adding $app's configuration files..."

ynh_config_add --template="config.js" --destination="$install_dir/js/config.js"

# FIXME: this should be handled by the core in the future
chmod 644 "$install_dir/js/config.js"
chown "$app:$app" "$install_dir/js/config.js"

#=================================================
# SYSTEM CONFIGURATION
#=================================================
ynh_script_progression "Adding system configurations related to $app..."

# Create a dedicated NGINX config using the conf/nginx.conf template
ynh_config_add_nginx

#=================================================
# END OF SCRIPT
#=================================================

ynh_script_progression "Installation of $app completed"
